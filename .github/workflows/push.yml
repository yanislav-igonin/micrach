name: Push

on: [push, release]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Install Go
      uses: actions/setup-go@v2
    - name: Build
      run: go build .
    - name: List dir
      run: ls

